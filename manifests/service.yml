apiVersion: v1
kind: Service
metadata:
    name: "ska"
    labels:
        app: "ska"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "ska"